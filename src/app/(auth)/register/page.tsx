import { RegisterForm } from "@/components/auth/RegisterForm";

export const metadata = {
  title: "Create Account — JWTelecoms",
  description: "Create your JWTelecoms account to get started",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
