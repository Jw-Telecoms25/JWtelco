import { LoginForm } from "@/components/auth/LoginForm";

export const metadata = {
  title: "Login — JWTelecoms",
  description: "Sign in to your JWTelecoms account",
};

export default function LoginPage() {
  return <LoginForm />;
}
